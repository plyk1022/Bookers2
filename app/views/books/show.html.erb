<h2>User info</h2>

<%= image_tag @user.get_profile_image(100, 100) %>
<table>
  <tbody>
    <tr>
      <td>name</td>
      <td><%= @user.name %></td>
    </tr>

    <tr>
      <td>introduction</td>
      <td><%= @user.introduction %></td>
    </tr>
  </tbody>
</table>

<%= link_to '（アイコン）編集ページ', edit_user_path(current_user.id) %>


<h2>New book</h2>
<%= form_with model: @book do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body, 'Opinion' %>
  <%= f.text_area :body %>


  <%= f.submit 'Create book' %>

<% end %>



<h2>Book detail</h2>

  <table>
    <tbody>
      <tr>
        <td>
          <%= link_to user_path(@selected_book.user.id) do %>
            <%= image_tag @selected_book.user.get_profile_image(100, 100) %>
          <% end %>
        </td>
        <td><%= @selected_book.title %></td>
        <td><%= @selected_book.body %></td>
        
        <% if @selected_book.user == current_user %>
          <td><%= link_to 'Destroy', book_path(@selected_book.id), method: :delete %></td>
        
        <% end %>
      </tr>
    </tbody>

  </table>




